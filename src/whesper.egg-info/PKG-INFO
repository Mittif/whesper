Metadata-Version: 2.4
Name: whesper
Version: 0.1.0
Summary: A Python-first CLI companion with multi-model routing.
Requires-Python: >=3.12
Description-Content-Type: text/markdown
Requires-Dist: prompt_toolkit<4,>=3.0.43

# whesper

A companion agent for immersive, adaptive intimate experiences.

## Docs

- [Whesper MVP 开发规格说明](docs/whesper_mvp_development_spec.md)

## CLI MVP

当前仓库已经包含一个 Python-first 的本地 CLI 聊天原型，重点是：

- 先把基础对话跑通
- 用 `TOML` 做多模型配置
- 支持多 provider / 多模型调度
- 本地会话历史落盘到 `.whesper/`

### Quickstart

1. 复制配置模板：

```bash
cp whesper.example.toml whesper.toml
```

2. 为 Kimi 设置环境变量：

```bash
export WHESPER_KIMI_API_KEY="your-api-key"
```

3. 启动 CLI：

```bash
python3 -m whesper --config whesper.toml chat
```

或者安装为命令：

```bash
python3 -m pip install -e .
whesper --config whesper.toml chat
```

### Useful Commands

- `/models`
- `/use local_chat`
- `/use auto`
- `/mode auto`
- `/mode reasoning`
- `/new test-session`
- `/history`
- `/exit`

### Enhanced CLI UX

当前交互已经朝 Claude Code 风格做了第一轮重构，依赖 `prompt_toolkit`，支持：

- `Up/Down` 浏览输入历史
- `Ctrl+R` 搜索历史
- `Tab` 做 slash command 自动补全
- 底部状态栏展示当前 `session / model / route`
- `/help`、`/use`、`/mode`、`/new` 等命令更适合交互式使用

### Multi-model Strategy

- 默认聊天使用 `scheduler.chat_model`
- 长输入或命中推理关键词时切换到 `scheduler.reasoning_model`
- `/search ...` 或 `search` 模式使用 `scheduler.search_model`

当前 provider 按 OpenAI-compatible API 统一接入，因此本地 `Ollama` 和远程 `Kimi` 都可以走同一套客户端。
